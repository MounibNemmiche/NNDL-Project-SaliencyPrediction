name: saliency
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch>=2.0.0
      - torchvision>=0.15.0
      - numpy>=1.24.0
      - Pillow>=9.5.0
      - PyYAML>=6.0
      - matplotlib>=3.7.0
      - tqdm>=4.65.0
      - scipy>=1.10.0
      - ipykernel>=6.0.0
      - jupyter>=1.0.0
